{% set name = "fenicsx-pulse" %}
{% set python_min = "3.10" %}
{% set version = "0.4.1" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/fenicsx_pulse-{{ version }}.tar.gz
  sha256: 159be4c03b4e97ce35bbaababb0dbede1d7aee03cb4b13b9bdbc03eb4b13077c

build:
  number: 0
  noarch: python
  entry_points:
    - "pulse=pulse.cli:main"
  script: python -m pip install -v --no-deps .

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - numpy
    - pint
    - rich
    - rich-argparse
    - scifem
    - fenics-dolfinx

test:
  requires:
    - python {{ python_min }}
    - pip
  imports:
    - fenicsx_pulse
  commands:
    - pulse --help
    - pip check


about:
  home: https://github.com/finsberg/fenicsx-pulse
  summary: Next generation cardiac mechanics solver based on FEniCSx
  license: MIT
  license_file: LICENSE
  doc_url: https://finsberg.github.io/fenicsx-pulse/
  dev_url: https://github.com/finsberg/fenicsx-pulse

extra:
  recipe-maintainers:
    - finsberg
